import 'package:flutter/material.dart';

class TimelineView extends StatefulWidget {
  final String title;
  const TimelineView({Key? key, required this.title}) : super(key: key);

  @override
  State<StatefulWidget> createState() {
    return TimelineViewWidget();
  }
}

class TimelineViewWidget extends State<TimelineView> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
